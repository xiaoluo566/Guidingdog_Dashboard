package com.nixiak.guidingdog_dashboard.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebSocket配置
 * Spring Boot会自动配置WebSocket支持
 */
@Configuration
public class WebSocketConfig {

    // Spring Boot 会自动配置 ServerEndpointExporter
    // 如果使用内嵌容器（Tomcat/Jetty），不需要手动配置

}

